<aside class="bg-dark">
    <div class="container text-center">
        <div class="call-to-action">
        	<% if user_signed_in? %>
						<h2>Welcome <%= current_user.email %></h2>
						<br>
						<br>
						<div data-no-turbolink><%= link_to 'Go to my calendar', events_path, :class => "btn btn-custom4 btn-lg", data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Loading calendar..."} %></div>
					<% else %>
            <h4>Ditch the pen and paper, spreadsheets, or inflexible software that you’re currently using</h4>
            <h4>and</h4>
            <h2>DOMINATE with DEFINITE6</h2>
            <%= link_to "DOMINATE Now!", new_user_registration_path, class: "btn btn-custom4 btn-lg wow tada", data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Loading..."} %>
          <% end %>  
        </div>
    </div>
</aside>

