//Hides the modal
$("#mynewscript").modal('hide');

//Clears the form
//$("#mynewscript .form-control").val('');

//Adds new script to the list of scripts
//$("#scripts-table").append('<%= j render @script %>');

//Reload because the datetime picker won't work after create (until after page is refreshed)
location.reload();
$(".overlay").show();