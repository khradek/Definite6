//Updates the hash in the Update Multiple modal
$("#hash-<%= @play.id %>").html('<%= @play.hash_mark %>');

//Updates the hash in the Remove Multiple modal
$("#remove-hash-<%= @play.id %>").html('<%= @play.hash_mark %>');