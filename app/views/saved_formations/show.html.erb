<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @saved_formation.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @saved_formation.user_id %>
</p>

<%= link_to 'Edit', edit_saved_formation_path(@saved_formation) %> |
<%= link_to 'Back', saved_formations_path %>
