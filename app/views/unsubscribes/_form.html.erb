<%= form_for(@unsubscribe) do |f| %>
  <% if @unsubscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unsubscribe.errors.count, "error") %> prohibited this unsubscribe from being saved:</h2>

      <ul>
      <% @unsubscribe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col-xs-12">
        <div class="field">
          <%= f.text_field :email, class: "form-control", placeholder: 'Enter email here' %>
        </div>
      </div>
    </div>

    <%= recaptcha_tags %>      

    <br>
    
    <div class="row">
      <div class="col-xs-12">
        <div class="actions">
          <%= f.button "Submit Form", class: "btn btn-custom2 btn-block", data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Submitting form..."} %>
        </div>
      </div>
    </div>

<% end %>
