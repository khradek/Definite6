<tbody id="saved_play_<%= saved_play.id %>">
  <tr>
    <td><%= saved_play.title %></td>
    <td><button type="button" id="update-saved_play_<%= saved_play.id %>" class="btn btn-custom4 btn-sm pull-right" data-toggle="modal" data-target="#myupdatesaved_play_<%= saved_play.id %>">
    	Update or Delete Play
    </button></td>
  </tr>
</tbody>

<div id="saved_play_update<%= saved_play.id %>">
	<!-- Modal - update saved_plays -->
	  <%= form_for(saved_play, :method => :put, remote: true) do |f| %>
	  <div class="modal fade" id="myupdatesaved_play_<%= saved_play.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
	  <div class="modal-dialog">
	    <div class="modal-content">
	      <div class="modal-header">
	        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	        <h4 class="modal-title" id="myModalLabel">Update Play</h4>
	      </div>
	      <div class="modal-body">

	      <div class="field">
	        <%= f.label :title %><br>
	        <%= f.text_area :title, class: "form-control" %>
	      </div>

	      <div class="modal-footer">
	      	<span class="pull-left" data-dismiss="modal" data-backdrop="false"> 
	      		<%= link_to 'Delete Play', saved_play, method: :delete, class: "btn btn-custom2", remote: true, data: { confirm: 'Are you sure?' } %>  
	    		</span> 
	        <button type="button" id="myupdatebutton_<%= saved_play.id %>" class="btn btn-default" data-dismiss="modal">Close</button>
	        <%= submit_tag "Update", class: "btn btn-custom1" %>
	      </div>
	    </div>
	  </div>
	  </div>
	  <% end %>
	<!-- Modal -->
</div>
