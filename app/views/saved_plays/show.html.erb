<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @saved_play.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @saved_play.user_id %>
</p>

<%= link_to 'Edit', edit_saved_play_path(@saved_play) %> |
<%= link_to 'Back', saved_plays_path %>
